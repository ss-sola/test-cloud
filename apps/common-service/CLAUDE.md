# CLAUDE

## 适用范围

- 本文件只维护 `apps/common-service` 共享包的专属约束。
- 通用工程约束继续遵守仓库根目录 `CLAUDE.md`。

## 共享包约束

- `apps/common-service` 作为共享包，文档按共享能力拆分到自身 `docs` 目录。
- 公共能力改动优先考虑对 `apps/*` 其他服务的连带影响。
- 后端服务的启动、日志、配置、通用中间件等公共能力优先在 `common-service` 内沉淀，再由业务服务复用，避免在业务服务内重复实现。
- 不把服务私有逻辑散落到公共层，也不把公共约束回写到单个业务服务。
