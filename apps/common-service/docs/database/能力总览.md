# 数据库接入能力总览

## 职责

公共数据库接入能力负责将 TypeORM 初始化逻辑挂载到应用模块。

## 核心实现

- `apps/common-service/src/database/enable-typeorm.decorator.ts`

## 当前定位

- 统一承载 PostgreSQL 的 TypeORM 连接初始化逻辑
- 统一从配置中心或本地配置中读取连接参数
- 避免业务服务各自重复装配 TypeORM
- 为各服务提供一致的实体扫描与数据库配置入口
