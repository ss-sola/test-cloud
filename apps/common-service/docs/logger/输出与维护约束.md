# 输出与维护约束

## 使用约束

- 业务代码统一通过 logger 输出日志
- 配置项变化时，应同步检查日志等级与输出行为文档
- 若新增日志目录、格式或采集链路，需要同步更新根级 `docs/project/部署与可观测性.md`
- 启动链路不得手工 `new AppLogger()`；统一通过 Nest 容器获取共享 logger，避免重复实例导致的进程级监听重复注册

## 维护边界

- 这里只记录共享日志能力
- 业务服务自身的日志文案或业务事件不记录在这里
