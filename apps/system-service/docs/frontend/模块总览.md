# 前端管理台模块总览

## 目标

`system-service` 为 NestCloud 提供统一的系统工作区，并托管三个无需后端接口的本地浏览器工具：图片转粒子、JSON 对比和 ENV 对比，同时提供通过后端读取 Git 提交的周报生成入口。工具页面与工作台总览共用左侧导航、顶栏和响应式布局；本地工具数据只在当前浏览器页面中处理，周报内容由服务端按白名单仓库生成并返回 Markdown。

## 资源位置

- 源目录：`apps/system-service/public/`
- 页面入口：`/public/index.html`
- 样式：`/public/css/app.css`
- 页面控制器：`/public/js/app.js`
- 纯逻辑与 Canvas 脚本：`/public/js/`
- 构建目录：`apps/system-service/dist/public/`
- 静态服务：`ServeStaticModule`，挂载路径为 `/public`

`nest-cli.json` 将源目录资源复制到构建目录，`dist/public/` 不作为源码维护。浏览器脚本使用 Node 可安全加载的自执行兼容封装，避免公共自动注册器扫描构建目录时访问 DOM。

## 页面入口

| Hash | 页面 | 说明 |
| --- | --- | --- |
| `#overview` | 工作台总览 | 系统管理台工作区首页 |
| `#particle` | 粒子动画 | 本地图片边缘粒子与 A/B 形态切换 |
| `#json-compare` | JSON 对比 | JSON 结构、身份数组和嵌套差异比较 |
| `#env-compare` | ENV 对比 | `.env` 字符串键值解析与差异比较 |
| `#weekly-report` | 周报生成 | 选择上周或本周，按白名单 Git 仓库生成 Markdown 汇总 |

空 hash、未知 hash 或非法 hash 会被规范化为 `#overview`。页面切换只改变当前视图，不整页刷新。

## 功能边界

- 图片、JSON 和 ENV 内容均在浏览器本地处理，不通过 `fetch`、XHR 或后端接口上传。
- 周报菜单通过 `POST /api/weekly-commit-reports/generate` 读取服务端配置允许的 Git 仓库；返回内容只在浏览器中展示、复制或下载，不写入用户指定的服务器路径。
- 周报未配置 AI 摘要服务时使用本地规则摘要；AI 密钥只通过服务端配置读取，不进入静态资源。
- 粒子动画只使用 Canvas、Image、ResizeObserver 和 Pointer Events；图片对象 URL 在替换、删除和页面销毁时释放。
- ENV 值始终保存为字符串，不执行变量展开、shell 命令或多行脚本。
- JSON 对比成功后，输入区按源项目行为回填为两空格缩进的规范化 JSON；ENV 对比保留用户输入的原始 `.env` 文本。

## 设计规范

页面遵循仓库根目录 `DESIGN.md` 的 `Flat Design Corporativo` 规范：使用蓝色主色、Lato 正文字体、JetBrains Mono 技术标识、8px 间距基准、4/8/12px 圆角层级和轻量阴影。源项目的深色渐变、强发光、重阴影和独立字体体系未迁移。

## 验证

纯解析和差异逻辑的回归测试位于 `apps/system-service/src/__tests__/frontend-logic.spec.ts`；浏览器 Canvas、拖放、响应式和键盘行为需要通过构建后的页面验收。