# 周报飞书单目标配置调整

- 开始时间：2026-09-02 16:11:18
- 结束时间：2026-09-02 17:33:03
- 耗时：约 1 小时 22 分钟
- 当前状态：已实现并完成自动化验证

## 背景

周报飞书发布当前使用 `monthlyTargets[]`，按周起始月份选择 Sheet，并允许调用方传入查找范围。需求改为单个 Wiki 地址和姓名，查找范围固定为 `A1:B200`，不再维护月份目标。

## 任务

1. 将请求 DTO、领域类型和 Feishu 发布设置改为单目标扁平结构。
2. 让发布服务直接使用 Wiki 地址，固定只读 `A1:B200`，保留姓名覆盖、唯一人员推断和 C:G 精确写入。
3. 简化管理台表单和 localStorage 草稿，兼容单项旧草稿并阻止多目标旧草稿自动发布。
4. 保留旧服务端配置读取迁移，新增 canonical `WeeklyReportFeishuTarget` 配置键。
5. 更新静态契约、DTO、发布回归测试及前端/模块文档。
6. 执行 lint、test、tsgo 并记录结果。

## 验收标准

- 新请求只含 `wikiUrl`/`name`（认证字段仍可选），不含 month、monthlyTargets、sheetId、lookupRange。
- 无论报告周跨不跨月，首次 Sheet 查找都为 `<sheetId>!A1:B200`。
- 页面没有月份、范围输入、添加月份和多目标模板。
- 旧多目标配置不会静默选择第一项或发起写入。
- 既有 staged 修改不被重置、不提交。
