# 移除静态管理台字典与菜单入口

## 背景

字典管理和菜单路由此前只是静态管理台预留的前端骨架，`system-service/src` 中没有对应后端模块。为避免页面继续暴露未实现功能，需要删除导航、hash 路由、隐藏视图和相关文档承诺。

## 任务清单

- [x] 删除静态页面中的字典管理、菜单路由导航和主体视图。
- [x] 删除总览中的模块快捷入口和模块统计，保留工作台总览。
- [x] 精简前端脚本，仅保留 `#overview`，未知 hash 回退总览。
- [x] 同步根 README、system-service README 和前端正式文档。
- [x] 更新初始化计划的当前结果描述。
- [x] 完成构建、静态资源和浏览器交互验证。
- [x] 执行 lint、test 和 tsgo。

## 时间记录

- 开始时间：2026-08-24
- 结束时间：2026-08-24
- 耗时：约 30 分钟
- 当前状态：已完成

## 验证记录

- 项目搜索确认静态页面和项目文档不再包含已移除模块入口。
- `pnpm --dir apps/system-service build`：通过，静态资源仍复制到 `dist/public`。
- HTTP 验证：`/public/index.html`、CSS、JS 返回 200。
- 浏览器验证：总览页面、移动端抽屉、未知 hash 规范化和无横向溢出通过。
- `pnpm run lint`：通过。
- `pnpm run test`：通过。
- `pnpm run tsgo`：通过。
